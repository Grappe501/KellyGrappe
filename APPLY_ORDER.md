# Apply Order

1. Replace each file in this package into the matching location in your repo.
2. From `app/`, run:
   - `npm run build`
3. If build passes, commit and push.
4. If build fails, send the full terminal output back exactly as printed.
