# Master Mapper V3

Replace your existing `mapper.py` with the included file.

## Run
```bash
pip install python-dotenv supabase
python mapper.py
```

## What it maps
- Supabase tables discovered from code references and common campaign tables
- Registry, kernel, circles, dashboards, modules, AI autobuild files
- Service implementation vs stub detection
- Recommended next build phase
