import { openDb } from "../../shared/utils/contactsDb"

const STORE_CONTACT_RELATIONSHIPS = "contact_relationships"

export async function linkContacts(params) {
  ...
}

export async function listContactRelationships(contactId) {
  ...
}