# APPLY_ORDER

1. Unzip into repo root.
2. Replace files exactly as provided.
3. Run:
   - `cd app`
   - `npm install` (only if package files changed)
   - `npm run build`
4. Push to git.
5. Let Netlify deploy.
6. Run smoke tests from TEST_PLAN.md.
7. Record outcome in BUILD_LEDGER.md.
